# @wave/ui

This package contains reusable UI components for the Wave project.

## Scripts

- `build`: Compiles the TypeScript source code.
- `test`: Runs the tests.

## Usage

Import and use the components in your project:

```ts
import { greet } from "@wave/ui";

console.log(greet("World"));
```